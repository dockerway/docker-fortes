query fetchTask($service: String){
    fetchTask(service: $service){
        id
        nodeId
        serviceId
        containerId
        nodeId
        image {
            name
            namespace
            domain
            fullname
            tag
        }
        createdAt
        updatedAt
        state
        message

    }
}
