query fetchService($stack: String){
    fetchService(stack: $stack){
        id
        name
        stack
        image {
            name
            namespace
            domain
            fullname
            tag
        }
        createdAt
        updatedAt
        ports{
            publishedPort
            targetPort
            protocol
        }
    }
}
