query fetchService($stack: String){
    fetchService(stack: $stack){
        id
        name
        stack
        image {
            name
            nameWithTag
            namespace
            domain
            fullname
            tag
        }
        createdAt
        updatedAt
        ports{
            hostPort
            containerPort
            protocol
        }
    }
}
