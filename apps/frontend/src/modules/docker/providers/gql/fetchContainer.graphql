query fetchContainer($service: String){
    fetchContainer(service: $service){
        id
        name
        task
        nodeId
        image {
            name
            namespace
            domain
            fullname
            tag
        }
        createdAt
        state
        status
        command
    }
}
